{% extends '_layout' %}

{% block main %}
	
	{% set homeBanner = {
		subheading: entry.subheading,
		copy: entry.copy
	} %}
	{% include "_partials/entry/homeBanner" %}
	
	{% set gallery = {
		images: entry.gallery.all()	
	} %} 
	
{% endblock %}